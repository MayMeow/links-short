services:
  nginx:
    image:
    container_name: links-proxy
    volumes_frop:
      - app
    ports:
      - "8084:80"
    depends_on:
      - app
      
  app:
    image: ghcr.io/maymeow/links-short/app:latest
    user: "11235:11235"
    container_name: links-app
    volumes:
      -./data:/data